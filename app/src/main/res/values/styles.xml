<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">-->
    <style name="TuTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorTuPrimary</item>
        <item name="colorPrimaryDark">@color/color_jd_red</item>
        <item name="colorAccent">@color/colorTuAccent</item>

        <!-- 默认状态状态设置底线颜色 -->
        <item name="colorControlNormal">#FFFFFF</item>
        <!-- 选择的底线颜色 -->
        <item name="colorControlActivated">#FFFFFF</item>
    </style>

    <style name="TuCompatTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <item name="colorControlNormal">@color/colorTu2</item>
        <item name="colorControlActivated">@color/color_jd_red</item>
    </style>
</resources>
